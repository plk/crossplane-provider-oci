apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Uncomment the database type you want to deploy:
  
  # Option 1: Traditional DB System (VM-based)
  # - claim-dbsystem.yaml
  
  # Option 2: Autonomous Database (Serverless - Paid)
  # - claim-autonomousdb.yaml
  
  # Option 3: Autonomous Database (Always Free Tier)
  - claim-autonomousdb-freetier.yaml